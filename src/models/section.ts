enum Section {
  Home,
  Projects,
  Skills,
  Experience,
}

// const length = Object.keys(Section).filter(
//   (val) => typeof val === "number"
// ).reduce((count, val) => count + 1, 0);

// export {length, Section};

export default Section;
